{
  "version": 2,
  "buildCommand": "cd web && npm install && NEXT_PUBLIC_APP_VERSION=\"$(cat ../VERSION)\" npm run build && cd .. && rm -rf web_dist && cp -R web/out web_dist && rm -rf web/node_modules web/.next web/out",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/app.py"
    }
  ]
}
